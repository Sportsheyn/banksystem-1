plugins {
    id 'java-library'
    id 'application'
}

application {
     mainClass = 'myproject.Main'
}


repositories {
     mavenCentral()
}

dependencies {
 implementation group: 'org.apache.commons', name: 'commons-lang3', version: '3.9'
 testImplementation group: 'junit', name: 'junit', version: '4.12'
}

jar {
 manifest {
 attributes "Main-Class": "myproject.Main"
 }
 from {
 configurations.runtimeClasspath.collect { it.isDirectory() ? it : zipTree(it) }
 }
}

task myZip(type: Zip) {

    //Project directory
    from './build'

    //Directory to output ZIP file
    destinationDirectory = file('C:\\Users\\chris\\Desktop\\Tutorial')

    //File name to archive
    archiveFileName = "testZip4.zip"
    //excludes  ['*/shop/product/data/' ]
    //includes = ['build\\docs']
}
